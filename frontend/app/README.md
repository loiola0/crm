# Focar Lab CRM Frontend

React + Vite application for the Focar Lab CRM.

## Useful commands

```bash
npm install
npm run dev
npm run build
```

## Environment

Create `frontend/app/.env.local` if you want to point the frontend directly to a non-proxied API:

```env
VITE_API_URL=http://localhost:8080/api
```
